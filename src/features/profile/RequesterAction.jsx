import ActionButton from "./ActionButton";

const RequesterAction = ()=>{
    return(
        <ActionButton>
            Cancel Request
        </ActionButton>
    );
}

export default RequesterAction;